# ypprototypes

Static-artifact deploy target for v2-front prototypes. Do not edit by hand.

Each `ptp/<name>` branch holds the built `dist/` of one prototype, force-pushed by `/deploy-prototype` from `v2-front`. `main` carries shared host config (`wrangler.toml`, `.assetsignore`) plus a placeholder `index.html`.
